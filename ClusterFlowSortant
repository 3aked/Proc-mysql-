BEGIN 
  DECLARE dist INT;
  SELECT COUNT( * ) INTO dist  
					FROM `CalaisLocation`
					WHERE (`heure` =X)
					AND(`year`=2017)
					AND(mois=5)
					and (jour =21)
and `id station arrivée` in (1,2,4,5,6,7,8,15,15,16,20,22,24);
  RETURN dist;
END
